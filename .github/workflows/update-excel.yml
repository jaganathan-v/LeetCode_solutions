name: Update LeetCode Analytics Excel

# Two ways this runs:
#  1. "push"     — instantly, whenever LeetSync pushes a new LeetCode solve.
#  2. "schedule" — once a day, to pick up any new GeeksforGeeks solves
#     (GFG has no push-based sync tool, so this is the closest thing to
#     "automatic" for that platform — expect up to a ~1 day delay there).
# concurrency queues overlapping runs instead of letting them race.
# On a push conflict, we don't try to git-merge the binary Excel file
# (that always fails) — we discard the stale commit, resync to the
# latest main, rebuild the report fresh, and recommit.
on:
  push:
    branches: [main]
    paths-ignore:
      - 'reports/**'
  schedule:
    - cron: '30 2 * * *'   # ~8:00 AM IST daily — edit if you want a different time
  workflow_dispatch: {}

concurrency:
  group: leetcode-report-build
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  build-excel:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo (full history needed for commit stats)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install openpyxl requests

      - name: Regenerate the Excel report
        run: python scripts/build_excel.py

      - name: Commit and push updated report (rebuild fresh on conflict)
        run: |
          git config user.name "leetcode-analytics-bot"
          git config user.email "actions@users.noreply.github.com"

          commit_and_push() {
            git add reports/LeetCode_Analytics.xlsx scripts/tag_cache.json scripts/gfg_seen.json
            if git diff --cached --quiet; then
              echo "No changes to commit."
              return 0
            fi
            git commit -m "Auto-update LeetCode/GFG analytics [skip ci]"
            git push
          }

          if commit_and_push; then
            exit 0
          fi

          for i in 1 2 3 4 5; do
            echo "Push failed — resyncing and rebuilding fresh ($i)..."
            git merge --abort 2>/dev/null || true
            git rebase --abort 2>/dev/null || true
            git reset --hard HEAD~1 2>/dev/null || true
            git fetch origin main
            git reset --hard origin/main
            python scripts/build_excel.py
            if commit_and_push; then
              exit 0
            fi
            sleep 5
          done

          echo "Failed to push after retries."
          exit 1
